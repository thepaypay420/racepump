# Production Neon Postgres connection string
# Example format: postgres://user:password@ep-xxx-yyy.neon.tech/dbname?sslmode=require
DATABASE_URL=postgres://USER:PASSWORD@YOUR-NEON-HOST.neon.tech/YOUR_DB?sslmode=require

# Persist SQLite cache between restarts (Replit Deployments use /mnt/data or /data)
# Prefer /mnt/data on Replit Deployments, else /data
# Canonical filename is pump-racers.db (legacy workspace.db will be migrated)
DB_PATH=/mnt/data/pump-racers.db

# Optional: deployment flags
# NODE_ENV=production
# REPLIT_DEPLOYMENT=true
